namespace UCosmic.Domain.Identity
{
    public static class MustNotHaveEmptyPrincipalIdentityName
    {
        public const string FailMessage = "User principal must have non-empty identity name.";
    }
}
