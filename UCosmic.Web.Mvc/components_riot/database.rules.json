{
  "rules": {
    "Members": {
      "$est_id": {
        ".read": "true",
        ".write": "auth != null && (auth.role[0].name == 'Institutional Student Supervisor' && auth.role[0].for_establishment == $est_id || auth.is_super == true)"
      }
    },
    ".read": "true"
  }
}