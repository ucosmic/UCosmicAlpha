@using UCosmic.Web.Mvc.ApiControllers
@model UCosmic.Web.Mvc.Models.PersonViewModel

@* EMAILS *@
<section class="content fixed to-top">
    @if (Model != null && Model.EmailAddress != null)
    {
        <h2 style="font-size: 24px; padding-bottom: 1em;">Emails</h2>
        <div>
            <ul>
                @if (Model.EmailAddress != null)
                {
                    <li><strong>@Model.EmailAddress</strong><span style="color: gray;"> (default)</span></li>
                }
                @foreach (var emailAddress in Model.EmailAddresses)
                {
                    if (emailAddress != null)
                    {
                        <li>@emailAddress</li>
                    }
                }
            </ul>
        </div>
    }
</section>
